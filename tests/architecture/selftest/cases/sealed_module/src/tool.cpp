// Stands in for a target created mid-migration (backend-real-core-tests and
// friends): an executable that links the sealed module and that nobody
// remembered to declare as a consumer.
int main() { return 0; }
